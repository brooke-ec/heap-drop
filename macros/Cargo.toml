[package]
name = "heap-drop-macros"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Derive macro for HeapDrop trait"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
proc-macro2 = "1"
quote = "1"
